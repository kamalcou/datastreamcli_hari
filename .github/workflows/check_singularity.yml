name: Build, Push DataStream Singularity Containers on X86, ARM
on:
  workflow_dispatch:
    inputs:
      branch_name:
        description: 'Branch name to build from (leave empty to use current branch)'
        required: false
        default: ''
        type: string
      force_build_deps:
        description: 'Force build datastream-deps (provide tag version, e.g., 1.0.0)'
        required: false
        default: ''
        type: string
      force_build_ds:
        description: 'Force build datastream (provide tag version, e.g., 1.0.0)'
        required: false
        default: ''
        type: string
  push:
    branches:
      - main
      - Datastream_Workflows

    paths:
      - 'versions.yml'

permissions:
  contents: read
  security-events: write

jobs:
  detect-changes:
    runs-on: ubuntu-latest
    outputs:
      build_deps: ${{ steps.changes.outputs.build_deps }}
      build_ds: ${{ steps.changes.outputs.build_ds }}
      deps_version: ${{ steps.changes.outputs.deps_version }}
      ds_version: ${{ steps.changes.outputs.ds_version }}

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        fetch-depth: 2

    # - name: update the ubuntu and install go 
    #   run: |
    #     sudo apt-get update && sudo apt-get install -y \
    #     build-essential \
    #     libssl-dev \
    #     uuid-dev \
    #     libgpgme11-dev \
    #     squashfs-tools \
    #     libseccomp-dev \
    #     pkg-config

    #     export VERSION=1.11 OS=linux ARCH=amd64 && \
    #     wget https://dl.google.com/go/go$VERSION.$OS-$ARCH.tar.gz && \
    #     sudo tar -C /usr/local -xzvf go$VERSION.$OS-$ARCH.tar.gz && \
    #     rm go$VERSION.$OS-$ARCH.tar.gz
    # - name: go version check
    #   run: |
        
    #     echo 'export GOPATH=${HOME}/go' >> ~/.bashrc && \
    #     echo 'export PATH=/usr/local/go/bin:${PATH}:${GOPATH}/bin' >> ~/.bashrc && \
    #     source ~/.bashrc
    #     go version
    # - name: go dependencies
    #   run: | 
    #     go get -u github.com/golang/dep/cmd/dep
    # - name: Download Singularity repo 
    #   run: | 
    #    go get -d github.com/sylabs/singularity
    # - name: Compile Singularity
    #   run: | 
    #    ./mconfig && \
    #     make -C ./builddir && \
    #     sudo make -C ./builddir install
    - name: Install singularity with apt-get
      run: |
        # sudo apt-get update && sudo apt-get install -y \
        # build-essential \
        # libssl-dev \
        # uuid-dev \
        # libgpgme11-dev \
        # squashfs-tools \
        # libseccomp-dev \
        # pkg-config
        
        sudo apt-get install -y singularity-container
        singularity --version
    - name: Build singularity image file
      run: |
       ./singularity/build.sh
      
    - name: run datastream.sif with singularity 
      run: |
        
        singularity exec datastream.sif python3 -c "import datastreamcli; print('OK')"
        singularity exec datastream.sif /datastreamcli/scripts/datastream --help

    

         
